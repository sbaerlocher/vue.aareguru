---
name: Weekly Security Scan
run-name: |
  Security Scan by @${{ github.actor }}

"on":
  schedule:
    - cron: "0 2 * * 1" # Weekly Monday 02:00 UTC
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write
  pull-requests: write

jobs:
  code-analysis:
    name: SAST (CodeQL)
    uses: sbaerlocher/.github/.github/workflows/security-code.yml@2026-06-21
    with:
      languages: '["javascript-typescript"]'
      node-version-file: ".nvmrc"
      concurrency-suffix: code

  dependency-scan:
    name: Dependencies & Licenses
    uses: sbaerlocher/.github/.github/workflows/security-deps.yml@2026-06-21
    with:
      language: "javascript"
      enable-license-check: true
      concurrency-suffix: deps

  secret-detection:
    name: Secret Detection
    uses: sbaerlocher/.github/.github/workflows/security-secrets.yml@2026-06-21
    with:
      concurrency-suffix: secrets
